[project]
name = "routepilot-benchmark"
version = "0.1.0"
description = "Deterministic benchmark for the RoutePilot-1B course"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"
